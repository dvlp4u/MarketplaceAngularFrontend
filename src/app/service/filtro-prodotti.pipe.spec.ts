import { FiltroProdottiPipe } from './filtro-prodotti.pipe';

describe('FiltroProdottiPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProdottiPipe();
    expect(pipe).toBeTruthy();
  });
});
